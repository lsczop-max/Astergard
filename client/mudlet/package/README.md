# Package staging

Ten katalog służy jako miejsce pośrednie do budowania release'u klienta Mudlet.

- źródła Lua są w `src/`
- gotowa mapa JSON trafia do `maps/`
- release `.mpackage` trafia do `releases/`
